name: penguin_android_camera_example
description: Demonstrates how to use the penguin_android_camera plugin.
publish_to: 'none'

environment:
  sdk: ">=2.12.0-0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  path_provider: ^1.6.27
  penguin_android_camera:
    path: ../
  permission_handler: ^5.0.1+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  lint: ^1.5.1

flutter:
  uses-material-design: true
