name: penguin_android_camera
description: A new flutter plugin project.
version: 0.0.1
author:
homepage:

environment:
  sdk: ">=2.12.0-0 <3.0.0"
  flutter: ">=1.20.0"

dependencies:
  reference:
    path: ../reference
  flutter:
    sdk: flutter

dev_dependencies:
  # TODO: global repo analysis file
  lint: ^1.5.1
  reference_generator:
    path: ../reference_generator
  build_runner: ^1.10.13
  flutter_test:
    sdk: flutter

flutter:
  plugin:
    platforms:
      android:
        package: github.bparrishMines.penguin.penguin_android_camera
        pluginClass: PenguinAndroidCameraPlugin
