name: penguin_usage
description: A new flutter plugin project.
version: 0.0.1
author:
homepage:

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  penguin:
    path: ../penguin/
  build_runner: ^1.6.7
  penguin_generator:
    path: ../penguin_generator/
  flutter_test:
    sdk: flutter

flutter:
  plugin:
    androidPackage: com.example.penguin_usage
    pluginClass: PenguinUsagePlugin
